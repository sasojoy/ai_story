# 征服路線重構計畫 (Conquest Route Redesign)

> 本文件記錄一次遊戲方向的重大轉向：從現有的「5 類選項＋4 種全域結局＋5 章勢力劇情」大型沙盒，
> 收斂成「單一情慾/征服路線＋4 位獨立女主角，各自靠好感度門檻觸發專屬結局」的聚焦設計。
> 這份文件是**規劃階段的產出**。三、四、五.1 節（人設、世界觀、好感度資料結構/結局判定/
> npcs.json 新欄位）都已經跟使用者逐項確認定案，**第一輪實作已完成**（核心程式碼、五份主要
> 設定檔、測試套件全數更新，63 個測試通過），見第八節。
> 開發分支：`feature/conquest-route-redesign`。

## 一、為什麼要做這個轉向

這個 session 花了大量時間在打「`qwen2.5:1.5b` 常態性不穩定」這個問題（復讀迴圈、JSON 截斷、
选项脱离劇情、fallback 觸發率一度接近 100%），根因之一是現有系統對這個小模型要求太多：
每回合要同時處理 5 種完全不同類型的選項、4 種全域結局的判定條件、5 章的勢力/劇情脈絡、
一次 JSON 要填 ~16 個欄位。

收斂成單一路線，除了是內容方向的選擇，**也是一次降低模型負擔的架構簡化**：
- 選項不用再涵蓋「正派/謀略/混亂/探索」四種完全不同調性，全部圍繞同一個主題發想
- 結局判定改成單一、對稱、可預測的規則（好感度門檻），不用 LLM 自己判斷是否達成結局
- 好感度增減暫時**不靠 LLM 每回合自己判斷**，改用預先定義好的固定值——這是目前最大的一項簡化，
  直接砍掉一個過去一直不穩定的環節，等模型/硬體條件更好後再考慮恢復成動態判斷

## 二、核心機制（已確認）

- **單一路線**：只有情慾/征服路線，不再有 A(正派)/B(謀略)/D(混亂)/E(探索) 四種選項類型。
  現有 `src/thread_state.py` 的主題線鎖定狀態機（為了在多類型選項間管理「鎖定同一主題」而設計）
  在單一路線下失去存在意義，新 session 應該**整個移除或大幅簡化**，不要保留這層複雜度。
- **四位女主角，可並行攻略**：不是選一位就排除其他三位，玩家可以同時或先後攻略全部四位；
  全部四位都觸發過結局（不論好結局或黑化結局）才算整場遊戲破關。
- **好感度區間 -50 ~ 80**（暫定數字，可調），每位角色各自獨立起始於 0。
  - 好感度 **達到 80**：觸發該角色專屬的「終極劇情」（正向結局，關係圓滿收尾）
  - 好感度 **降到 -50**：觸發該角色專屬的「黑化結局」——**這不是單純的失敗畫面**，而是敘事上的
    另一種走向（例如主角對其強行壓制/用強），基調依角色個性各自不同，見下方角色設定
- **好感度增減：預先定義固定值，不靠 LLM 判斷**（已確認，近期方向）。每個選項在內容資料裡
  就要標好固定的好感度增減值，LLM 的工作縮小成「照著這個已知結果去寫敘事」，不用自己判斷
  +/- 多少。等模型更穩定、或換了更好的本機硬體/模型之後，才考慮恢復成動態判斷。
- **世界觀維持暗黑武俠風，但劇情整套換新**：不延續現有的龍門客棧/血秘卷/血衣樓故事，
  地點、勢力、主線衝突全部重寫。

## 三、四位女主角（已定案）

使用者已逐一過目確認四位人設（含黑化流程細化、身材數值），細節見下方。**人設定案，
可以開始下一步（世界觀第四節確認 → 技術架構落地）。**

**黑化結局的本質已由使用者釐清（重要修正）**：好感度降到 -50 觸發的「黑化結局」**不是**角色
反過來對抗/傷害主角，而是主角用武力壓制、脅迫或用計得逞，把該角色收服、調教成臣服於主角的
女奴（性奴隸）從屬關係——四人的結局「狀態」本質上是一致的。下面四人的「黑化方向」保留的是
**角色差異化的敘事切入點**：主角用什麼手段制服她、她被制服後性格上如何反應／是否仍保留一絲
原本個性的抵抗，而不是「她贏了/她反擊成功」。**實際劇情文字（含親密場景）不在這次架構規劃
或一般模型的實作範圍內，使用者已決定之後改用無審查模型另外處理，見第六節。**

**1. 沈青鋒（冷豔劍客）**
一劍宗年輕宗主，武林公認第一女劍客，出身名門正派。孤傲清冷、極度自律，把武學當信仰，
瞧不起用陰謀詭計的人。生平：幼年目睹師門長輩死於暗算，從此不再輕信他人，把情感封進劍裡。
會被主角的真本事而非花言巧語打動。體質設定：身體其實極度敏感（跟她表面的冷硬人設形成反差，
她自己也未必清楚/承認這點）。黑化方向（已細化為具體流程）：主角先下迷藥，再激怒她拔劍相向，
在戰鬥過程中趁隙凌辱她、利用她敏感體質的落差擊潰她的心防，最終使她臣服——不是單純武力壓制，
而是「藥物＋戰鬥＋凌辱」三個步驟疊加的具體橋段。傲骨被折損的反差仍是這條線的核心看點。
身材：174cm，劍客體態，修長勁瘦、肌肉線條緊實但不誇張，三圍 B85-W58-H86，長腿窄腰，
氣質偏冷硬俐落，不走豐滿路線。

**2. 慕容茵（心機毒女）**
毒經谷谷主之女，表面溫婉大方、實則城府極深，擅長用美色與情報操縱人心，四人中最會反過來
算計主角的一位。生平：從小被當政治聯姻棋子教養長大，早看穿江湖虛偽，對真心毫無信任感，
習慣用交易心態看待所有關係。真正攻略她需要讓她相信「這次不是交易」。黑化方向：她想反過來
設局算計主角，一旦算計被識破、反被將計就計拿下，向來精於算計的她第一次徹底無計可施地臣服。
身材：165cm，豐滿妖嬈、身段柔軟，慣用體態魅惑人心，三圍 B92-W60-H90，曲線明顯，
走的是媚而不俗的貴女路線。

**3. 卓芷若（溫柔藥師）**
山莊駐地女醫，家傳醫術，溫柔堅韌，四人中情感最柔軟、最容易對主角心軟，但對「暗黑手段」有
強烈道德底線。生平：家人死於瘟疫，發誓行醫濟世、絕不再讓人枉死，對主角身上的殺伐之氣既恐懼
又忍不住想拯救。全劇最接近「救贖」基調的一條線。黑化方向：強攻她的黑化最沉重，道德底線被
徹底踐踏擊碎，從溫柔堅韌變成必須依附主角才能感到安全的樣子。
身材：162cm，溫婉柔和身形，不誇張但有女人味，三圍 B86-W60-H88，體態偏纖細軟糯，
給人「照顧起來讓人心疼」的柔弱感。

**4. 阿罌（狂放邪修）**
血罌宗年輕巫女，野性不羈、我行我素，把情慾與武學修煉直接掛鉤（類似現有的雙修設定），
四人中最主動、最不需要「攻略」但最難「馴服/駕馭」。生平：從小在邪修祕境長大，沒有江湖規矩
的概念，唯一信奉強者為尊。黑化方向：**已確認採用 (a)**——不再是「反噬失控」的特例，改成
跟其他三人一致：被主角壓制臣服，只是她生性最野、最不服馴，制服過程比其他三人更費力、
拖得更久，是四人中「最晚被真正馴服」的一位，而不是機制上的例外。
身材：156cm，嬌小豐滿的成年女性體型（骨架、五官、氣質維持成年比例，單純個子矮，
不是孩童身形），三圍 B94-W56-H88，巨乳與野性氣質形成反差，是四人中身材尺寸最誇張的一位。

## 四、世界觀提案（已定案）

主角是沒落刺客世家「風隱谷」的末代傳人，家族因十年前一樁滅門懸案幾乎絕跡，主角身上帶著
半本記載「奪魄迷心錄」的禁忌武學殘卷，完整版下落牽動江湖四大勢力。武林即將召開十年一度的
「群芳會」，四大勢力各自派出核心女性代表齊聚「棲霜山莊」，明為比武交流、實為爭奪殘卷與
勢力版圖。主角受邀（或潛入）赴會，須在山莊逗留期間逐一接近、收服四位女子，換取她們手中的
勢力資源與殘卷線索。

**四大勢力對應（已確認）**：卓芷若的「山莊駐地女醫」設定跟其他三位「受邀赴會的外部勢力
代表」性質不同，解法是把**棲霜山莊本身**也算一方勢力（東道主兼參與者），卓芷若代表山莊/
主辦方立場，其餘三位是受邀赴會的外部勢力代表：
- 一劍宗 → 沈青鋒（正派劍術）
- 毒經谷 → 慕容茵（毒功/情報操縱）
- 血罌宗 → 阿罌（邪修/雙修）
- 棲霜山莊（東道主） → 卓芷若（駐地女醫，代表主辦方）

## 五、技術架構影響（給新 session 的實作指引）

依現有程式碼結構（`src/game_engine.py` facade + `src/rules.py`/`src/state.py`/`src/npc_agent.py`
分層，見 `ARCHITECTURE.md`），這次重構預期會動到：

- **`src/thread_state.py`**：移除或大幅簡化。單一路線下不再需要「鎖定某個類別主題」這個概念。
- **`config/npcs.json`**：現有 4 個 NPC（含兩位男性：殺手阿福、錢莊老王）整個替換成新的 4 位女主角。
- **`config/npc_fallbacks.json`**：為新角色重寫保底劇情/選項內容。
- **`config/story_outline.json`**：現有的 5 章劇情 + 4 種全域結局（`evaluate_ending` 的寫死條件）
  需要整個替換。新結局判定應該是**對稱、可重用的規則**：對每一位角色分別檢查
  「好感度 >= 80 → 觸發她的好結局」「好感度 <= -50 → 觸發她的黑化結局」，而不是像現有
  `rules.py::evaluate_ending` 那樣寫 4 條各自獨立、條件互不相同的 if 判斷。5 章分章節奏
  （`turns_range` 那套）**已確認保留**，當作淡淡的背景節奏（章節標題/goal 文字餵給 system
  prompt 當敘事氛圍提示），但不再驅動任何判定邏輯——結局只看好感度門檻，章節進度跟結局
  觸發完全脫鉤。
- **`src/npc_agent.py::build_system_prompt`**：選項生成指令要大幅簡化——不再需要 A~E 五種類型
  說明，改成圍繞單一角色的情慾/征服脈絡發想選項。這是預期能明顯降低 prompt 複雜度、
  改善模型穩定度的關鍵改動之一。
- **好感度增減的資料結構**：需要新設計一套「選項 → 固定好感度增減值」的資料表達方式
  （例如選項池資料裡每個候選字串旁邊直接帶一個數字），取代現有靠 `GameStateDelta.intimacy_change`
  讓 LLM 每回合自己回報的做法。這是這次重構的核心機制變動，需要仔細設計資料結構。
- 現有的 `src/save_manager.py`/`src/state.py` 存讀檔邏輯、Web UI 串流機制等基礎設施可以沿用，
  不預期需要重寫。

## 五.1、好感度資料結構與角色設定檔具體設計（已確認方向）

### 核心轉變：分類標籤（tag）定值，選項文字仍交給 LLM 動態寫，每回合 3 個選項

**放棄「靜態選項池」方案**：原本設計是把好感度值直接綁在預先寫死的候選文字上（每個候選
字串旁邊帶一個固定數字）。分析後發現這會重演這個專案已經踩過的雷——CLAUDE.md 記錄過
「選項解析失敗時保底機制被觸發，玩家反饋選項像是寫死的」這個嚴重 bug；靜態選項池等於把
「選項僵化」這個體驗從「只在解析失敗時出現的保底情境」升級成**每回合的常態**，而且要撐住
一整輪好感度從 0 爬到 80（或掉到 -50）需要準備大量候選文字，內容維護成本高。

**改採「tag 分類定值」**：查表對象從「選項文字」改成「選項分類」，文字仍由 LLM 每回合動態
生成（貼合當下劇情，不僵化），但數值 100% 由系統查表決定：

1. **好感度區間改成 -50 ~ 80**：`NPCProfile.intimacy`（`src/models.py`）與 `apply_delta`
   （`src/rules.py`）的 clamp 從 `max(0, min(100, ...))` 改成 `max(-50, min(80, ...))`。
2. **每位角色定義 4-5 種固定的「切入方式」分類**，delta 存在分類上，不是存在每條候選文字上，
   放進 `config/npcs.json` 各角色資料裡（或獨立成 `config/npc_intimacy_tags.json`，實作時
   再定）：
   ```json
   { "沈青鋒": { "真誠切磋": 6, "中性互動": 2, "花言巧語": -3, "強攻鋪墊": -8, "黑化臨界": -25 } }
   ```
3. **每回合顯示 3 個選項**（原本設計是 5 個，考量小模型輸出負擔跟玩家選擇疲勞後改小）。
   LLM 仍自己寫這 3 個選項的文字，但 JSON schema 要求每個選項附一個 `tag` 欄位，只能從該
   角色固定的分類清單裡選——這是**封閉小清單的分類判斷**，比「自己決定 +6 還是 +8」這種
   開放數值判斷對 `qwen2.5:1.5b` 友善很多，複雜度也比現在「自由發想完整選項清單」更低。
   引擎收到 tag 後查表拿 delta，無視/覆蓋 LLM 有沒有自己亂填數字；tag 不在清單內（小模型
   偶爾會亂寫）就 fallback 成中性分類、給一個小的預設值，不會讓整回合失敗。
4. **「黑化臨界／終極劇情」這種不可逆的關鍵分岔，不靠 LLM 自己想到要放進選項**：好感度接近
   門檻（例如 -50 的 -40 以內、80 的 70 以上）時，系統**決定性地取代其中一格**（例如把
   「中性互動」那格換成「黑化臨界」/「終極劇情臨界」），維持選項數固定是 3，玩家清楚知道
   自己正在選一個不可逆的轉折點，不是隨機從池子裡撞到的。
5. **回合流程**：玩家選定選項 →（`apply_delta` 前）呼叫端依該選項的 `tag` 查表拿到
   `intimacy_delta`，覆寫 `delta.intimacy_change`，不信任 LLM 自報的數字 → system prompt
   告知 LLM「玩家選了 [選項文字]（分類：[tag]），這會讓好感度 [+X/-X]，請依這個已知結果
   描寫接下來的劇情反應，不要自己改變好感度」。
   預期效益：好感度數值 100% 可預測、可測試，且不會犧牲選項文字的敘事新鮮感。
6. **現有的靜態保底池**（`src/options.py::generate_fallback_options`、
   `config/npc_fallbacks.json`）**維持不動**，繼續當「LLM 解析失敗」的最後防線——那是低頻
   情境，不會有僵化問題，不受這次改動牽連；保底池也要跟著從 5 個改成 3 個。
7. **UI 影響**：`web_ui.py` 目前是 5 個按鈕各自綁 `gr.State("A"~"E")`，要改成 3 個按鈕；
   `GameStateDelta.options` 的 schema 說明跟驗證邏輯（`is_placeholder_option` 等）也要跟著
   從 5 改成 3。這些是實作階段的連帶修改，不影響這裡的設計方向。

### 結局判定改成對稱規則（已確認）

`src/rules.py::evaluate_ending` 整個換掉：
```python
def evaluate_ending(state, story_outline):
    for npc_name, agent in state.agents.items():
        if npc_name in state.triggered_endings:
            continue
        if agent.profile.intimacy >= 80:
            return {"npc": npc_name, "type": "good", ...}
        if agent.profile.intimacy <= -50:
            return {"npc": npc_name, "type": "bad", ...}
    return None
```
新增 `state.triggered_endings: Set[str]`，記錄哪些角色已觸發過結局，避免同一角色重複觸發；
全破關條件＝四位角色都在 `triggered_endings` 裡（第二節「並行攻略、全部觸發過結局才算破關」）。

### `config/npcs.json` 新結構（四位女主角，已確認）

在現有欄位（`personality`/`biography`/`stats`/`relationships` 等）之外新增 `body`（身材）與
`bad_ending_flow`（黑化流程步驟，給 LLM 或之後的無審查模型當劇情大綱參考，不是要一般模型
自己寫露骨內容）：
```json
{
  "沈青鋒": {
    "name": "沈青鋒",
    "display_name": "青鋒",
    "identity": "一劍宗宗主",
    "faction": "一劍宗",
    "personality": "孤傲清冷、極度自律，把武學當信仰，瞧不起陰謀詭計。",
    "body": {
      "height_cm": 174,
      "build": "劍客體態，修長勁瘦，肌肉線條緊實但不誇張",
      "measurements_cm": { "bust": 85, "waist": 58, "hip": 86 },
      "sensitivity_note": "體質極度敏感，跟表面冷硬人設形成反差"
    },
    "bad_ending_flow": ["下迷藥", "激她拔劍相向", "戰鬥中凌辱她", "利用敏感體質擊潰心防", "臣服"],
    "biography": ["...沿用現有親密度分級解鎖生平機制..."],
    "stats": { "realm": "...", "attack": 0, "defense": 0, "agility": 0, "weapon": "..." }
  }
}
```
`config/npc_fallbacks.json`、`config/npc_stages.json` 也要為四位新角色重寫對應內容
（沿用現有格式，只是換人換內容）。

## 六、暫緩事項（尚未解決，不在這次重構範圍內）

- **結局劇情的實際文字內容（含親密場景）沒有做出來**：這個 session（跨兩次對話）測試過用
  本機模型（`qwen2.5:1.5b`、`qwen2.5:7b`、`huihui_ai/qwen3.5-abliterated` 4b/9b、
  `hf.co/bartowski/EVA-Qwen2.5-7B-v0.1-GGUF` 等）生成使用者提供範例尺度的親密場景，
  全部都沒有達到使用者要的露骨程度，9b 版本甚至因為這台機器記憶體只有 15.6GB（扣掉其他
  常駐程式後可用空間不到 1GB）而完全卡死。**使用者已決定這部分等之後加大機器記憶體、
  能跑更大的模型之後再處理**，不是這次架構重構要解決的問題。四人黑化結局的統一方向
  （主角武力/脅迫/用計制服角色、調教成女奴）已在第三節記錄，供之後接手寫實際劇情文字時參考。

  **這次額外做的事（管線已備妥，等之後模型/硬體到位即可直接用）**：新增獨立於一般回合制
  劇情之外的結局生成管線——`config/ending_writer_config.json`（獨立模型設定，跟
  `qwen2.5:1.5b` 的 `game_config.json` 分開，避免兩邊參數需求互相牽制）、
  `src/ending_writer.py::generate_ending_scene()`（組裝角色人設/身材/黑化流程大綱成
  prompt，呼叫指定模型生成長篇內文並存成 `saves/endings/{角色}_{good|bad}.txt`）、
  `src/ollama_client.py::chat_text()`（不強制 JSON schema 的純文字長篇生成路徑）、
  Web UI 新增「🔞 結局劇情檢視器」面板可即時讀檔顯示。**用 EVA-Qwen2.5-7B-v0.1-GGUF
  (Q4_K_M) 實測的結論**：篇幅足夠（近 2000 字），但角色名字會飄掉、劇情跟給定的黑化大綱
  前後不連貫——推測是 7B 模型在 Q4 量化下擕不住一次 2048 tokens 長篇生成的連貫性，
  漸漸飄回模型自己訓練資料裡的人名/劇情，不是量夠不夠露骨的問題，是連貫性本身就不夠。
  **使用者決定先停在這裡**，不繼續投入「拆成多次較短呼叫、每步驟分別生成再接續」這類更大的
  架構調整，等之後硬體/模型條件更好再回頭處理。管線本身已經可用，之後只要換掉
  `ending_writer_config.json` 的 `model_name` 指向更強的模型即可，不需要重新搭建。
- 好感度數值區間（-50~80）、門檻是否要因角色而異，都還是初版暫定值，之後可以再調整。
- ~~是否要完全捨棄現有的 4 位舊 NPC 內容~~：第一輪實作已直接整個替換掉（`config/npcs.json`
  等設定檔已無殺手阿福/錢莊老王/合歡宗聖女/風騷老闆娘的內容），這個問題已不再存在。

## 七、與現有文件的關係

`VISION.md`/`ROADMAP.md`/`ARCHITECTURE.md` 描述的是現有那套「多軸線沙盒」願景，跟這次的
單一路線重構方向有實質衝突，**這次刻意不去改動那三份文件**，避免污染 `main` 分支的既有規劃。
等這次重構的方向真正定案、進入實作階段，再回頭決定要更新那三份文件還是讓這條產品線
（`feature/conquest-route-redesign`）長期獨立於 `main` 之外。**第一輪實作已完成（見第八節），
這個決定點已經到了，需要使用者拿主意。**

## 八、第一輪實作紀錄

依第五、五.1 節設計，這個 session 已完成以下實作，`pytest` 63 個測試全數通過：

- **`src/models.py`**：`GameStateDelta.intimacy_change` 改為系統覆寫、不信任 LLM；`options`
  改成 3 個 (A/B/C)；新增 `option_tags` 欄位與正規化邏輯。`NPCProfile` 新增 `faction`/`body`/
  `bad_ending_flow`/`intimacy_tags` 欄位，以及 `resolve_intimacy_delta()` 查表方法。
- **`src/rules.py`**：`apply_delta` 好感度 clamp 改成 `-50~80`；`evaluate_ending` 改寫成對稱、
  逐一檢查每位角色的通用邏輯，並記錄 `state.triggered_endings`；新增 `all_endings_triggered()`。
- **`src/state.py`**：新增 `triggered_endings` 欄位。
- **`src/thread_state.py`**：整個刪除（單一路線不再需要主題線鎖定狀態機）。
- **`src/npc_agent.py`**：移除主題線鎖定相關邏輯；新增 `last_offered_options`/`last_offered_tags`
  比對玩家這次選擇對應的分類，查表覆寫好感度變化並回饋「已知結果」給 LLM；system prompt 簡化
  成單一路線版本。
- **`src/options.py`**：保底選項池改成依分類 (tag) 而非舊版 A~E 位置類別；新增
  `inject_critical_option()`，好感度接近門檻時系統決定性插入「黑化臨界／終極臨界」選項。
- **`src/game_engine.py`** / **`src/save_manager.py`** / **`web_ui.py`** / **`main.py`**：
  移除 `chosen_category` 參數；Web UI 從 5 按鈕改成 3 按鈕；存讀檔同步 `triggered_endings` 與
  `last_offered_options`/`last_offered_tags`，不再存讀主題線欄位。
- **設定檔全面替換**：`config/npcs.json`（四位女主角，含身材、黑化流程、好感度分類定值）、
  `config/npc_fallbacks.json`、`config/npc_stages.json`（好感度分級門檻同步改成 -50~80 四段）、
  `config/story_outline.json`（章節改群芳會敘事，`endings` 改成逐角色 good/bad 結構）、
  `config/world_intro.json`、`config/world_map.json`（棲霜山莊五個地點）、
  `config/npc_autonomy.json`；`config/lorebook.json` 世界觀文字微調去除舊角色殘留引用。
- **測試套件**：`tests/test_models.py`/`test_options.py`/`test_engine.py`/`test_streaming.py`/
  `test_save_manager.py`/`test_ollama.py` 全數更新為新設定與新機制，移除主題線鎖定測試，
  新增好感度查表覆寫、關鍵臨界選項注入的測試。

**尚未做、留給下一步的事**：實際結局劇情文字（含親密場景）沒有生成，仍待第六節所述換用
無審查模型處理；沒有實機連 Ollama 跑過真的一整輪遊玩（這次只用 mock LLM 驗證邏輯正確性）；
`VISION.md`/`ROADMAP.md`/`ARCHITECTURE.md` 三份文件尚未更新，第七節的決定點還沒拍板。
